# Backend (ASP.NET Core API)

This folder will contain the ASP.NET Core Web API that handles document ingestion, embedding generation, semantic search, and calls to the LLM.

Suggested structure:
- src/
- Dockerfile

Run locally:
- dotnet restore
- dotnet run